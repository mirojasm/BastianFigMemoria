<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actividades Completadas</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #3f87a6, #ebf8e1);
            display: flex;
            justify-content: center;
            align-items: center;
            color: #2c3e50;
        }

        .completion-container {
            background-color: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 90%;
            text-align: center;
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .completion-icon {
            font-size: 5rem;
            color: #27ae60;
            margin-bottom: 1.5rem;
            animation: checkmark 0.5s ease-in-out 0.5s both;
        }

        @keyframes checkmark {
            0% {
                transform: scale(0);
            }
            50% {
                transform: scale(1.2);
            }
            100% {
                transform: scale(1);
            }
        }

        .completion-title {
            color: #2c3e50;
            margin-bottom: 1.5rem;
            font-size: 2.5rem;
            font-weight: bold;
        }

        .completion-message {
            color: #34495e;
            line-height: 1.6;
            margin-bottom: 2.5rem;
            font-size: 1.2rem;
        }

        .completion-buttons {
            display: flex;
            justify-content: center;
            gap: 1rem;
        }

        .btn {
            display: inline-block;
            padding: 1rem 2rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1.1rem;
            text-decoration: none;
            transition: all 0.3s ease;
            font-weight: 600;
        }

        .btn-primary {
            background-color: #3f87a6;
            color: white;
            box-shadow: 0 4px 15px rgba(63, 135, 166, 0.2);
        }

        .btn-primary:hover {
            background-color: #2c3e50;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(44, 62, 80, 0.3);
        }

        .btn-primary:active {
            transform: translateY(0);
        }

        /* Estilos responsivos */
        @media (max-width: 768px) {
            .completion-container {
                padding: 2rem;
                margin: 1rem;
            }

            .completion-title {
                font-size: 2rem;
            }

            .completion-message {
                font-size: 1.1rem;
            }

            .btn {
                padding: 0.8rem 1.5rem;
                font-size: 1rem;
            }
        }

        @media (max-width: 480px) {
            .completion-container {
                padding: 1.5rem;
            }

            .completion-title {
                font-size: 1.8rem;
            }

            .completion-message {
                font-size: 1rem;
            }

            .completion-icon {
                font-size: 4rem;
            }
        }
    </style>
</head>
<body>
    <div class="completion-container">
        <div class="completion-icon">✓</div>
        <h1 class="completion-title">¡Actividades Completadas!</h1>
        <p class="completion-message">
            Has completado exitosamente todas las actividades colaborativas.
            Recuerda si lo puedes imaginar, lo puedes programar...

            ¡Gracias por tu participación!
        </p>
        <div class="completion-buttons">
            <!-- <a href="/levels" class="btn btn-primary" onclick="clearRoomData()">Volver al Inicio</a> -->
            <a href="/menuretro" class="btn btn-primary" onclick="clearRoomData()">ir a retroalimentacion</a>
        </div>
    </div>

    <script>
        function clearRoomData() {
            sessionStorage.removeItem('currentRoom');
            localStorage.removeItem('currentActivity');
        }
    </script>
</body>
</html>